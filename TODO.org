- Remove the Rails.application.config.active_record.belongs_to_required_by_default = false config
- Create unique index in :uniqueness for ApplicationToken, TeamUser and Star
- Update JS packages
- webpacker and sprockets
- packaging
  - This also includes Dockerfile, docker/Dockerfile, etc.
- Submit PR
- Check multiple databases setups
- Check parallel testing
